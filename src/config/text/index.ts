import { commonText } from "@/config/text/common";
import { authText } from "@/config/text/auth";
import { dashboardText } from "@/config/text/dashboard";
import { ordersText } from "./orders";
export const text = {
  common: commonText,
  auth: authText,
  dashboard: dashboardText,
  orders: ordersText,
};
